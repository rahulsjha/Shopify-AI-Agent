# Frontend

React + Vite frontend for the Shopify AI agent.

## Run

1. Install dependencies: `npm install`
2. Start the app: `npm run dev`
3. If your backend is not on `http://localhost:8000`, set `VITE_API_BASE_URL` in a local `.env` file.
